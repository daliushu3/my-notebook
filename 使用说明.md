# 📝 我的笔记应用 - 使用说明

## 🎯 版本说明

我为你创建了**两个版本**的笔记应用，解决localStorage配额限制问题：

### 1️⃣ **IndexedDB网页版** (`note-app-indexeddb.html`)
- ✅ 在线使用，无需安装
- ✅ 存储空间更大（几百MB到几GB）
- ✅ 自动图片压缩功能
- ✅ 实时显示存储空间使用情况
- 📍 适合：继续部署到GitHub Pages或直接用浏览器打开

### 2️⃣ **Electron桌面版** (`electron-notes/`文件夹)
- ✅ 完全本地化，数据存储在电脑上
- ✅ 无存储空间限制
- ✅ 独立的桌面应用程序
- ✅ 自动图片压缩功能
- 📍 适合：需要完全离线使用或超大数据量

---

## 🚀 快速开始

### 方案1：使用IndexedDB网页版（推荐先试用）

1. **导出现有数据**
   - 打开你原来的 `note-app-completed.html`
   - 进入"设置"页面
   - 点击"📤 导出数据"，选择要导出的内容
   - 保存JSON文件到电脑

2. **使用新版本**
   - 直接用浏览器打开 `note-app-indexeddb.html`
   - 进入"设置"页面
   - 点击"📥 导入数据"，选择刚才导出的JSON文件
   - 完成！

3. **检查存储空间**
   - 在"设置"页面底部可以看到存储使用情况
   - 显示格式：`已使用: XXmb / XXXMB (X.X%)`

---

### 方案2：使用Electron桌面版

#### 📦 安装步骤

**Windows用户：**

1. **安装Node.js**
   - 访问 https://nodejs.org/
   - 下载并安装LTS版本

2. **安装应用**
   ```bash
   # 打开命令提示符(CMD)或PowerShell
   cd C:\Users\tween\droid\electron-notes
   npm install
   ```

3. **运行应用**
   ```bash
   npm start
   ```

4. **打包成安装程序（可选）**
   ```bash
   npm run build:win
   ```
   - 打包完成后，在 `dist` 文件夹里找到 `.exe` 安装文件
   - 双击安装即可

---

## 📂 文件结构说明

```
C:\Users\tween\droid\
├── note-app-completed.html      # 原版本（localStorage，有配额限制）
├── note-app-indexeddb.html      # IndexedDB版本（推荐）✨
└── electron-notes/              # Electron桌面版
    ├── package.json             # 项目配置文件
    ├── main.js                  # Electron主进程
    ├── preload.js               # 安全桥接文件
    ├── index.html               # 应用界面（需要复制）
    └── README.md                # 详细说明
```

---

## 🔧 Electron版本额外步骤

你需要将 `note-app-indexeddb.html` 的内容稍作修改后复制到 `electron-notes/index.html`：

### 修改保存/加载函数

在JavaScript代码中，将这些函数替换：

```javascript
// 原来的 saveData 函数替换为：
async saveData() {
    try {
        if (window.electronAPI) {
            // Electron环境
            await window.electronAPI.saveFile(this.data);
        } else {
            // 浏览器环境（IndexedDB）
            await this.db.saveData('noteAppData', this.data);
        }
        await this.updateStorageInfo();
    } catch (error) {
        console.error('保存数据失败:', error);
        throw error;
    }
}

// 原来的 loadData 函数替换为：
async loadData() {
    try {
        if (window.electronAPI) {
            // Electron环境
            const result = await window.electronAPI.loadFile();
            if (result.success && result.data) {
                this.data = result.data;
            }
        } else {
            // 浏览器环境（IndexedDB）
            const saved = await this.db.getData('noteAppData');
            if (saved) this.data = saved;
        }
    } catch (error) {
        console.error('加载数据失败:', error);
    }
}
```

---

## ✨ 新功能说明

### 1. 图片自动压缩
- 上传图片时自动压缩到合理大小
- 最大宽度/高度：1920px
- 压缩质量：80%（图集）、90%（头像）、85%（壁纸）
- 大幅减少存储空间占用

### 2. 存储空间监控
- 实时显示已使用和总配额
- 帮助你了解数据占用情况

### 3. 完全本地存储（Electron版）
- 数据存储在：`C:\Users\你的用户名\AppData\Roaming\my-notes-app\notes-data.json`
- 可以直接备份这个文件

---

## 🔄 数据迁移建议

### 从旧版本迁移到IndexedDB版本：

1. ✅ 打开旧版本，导出所有数据
2. ✅ 打开IndexedDB版本，导入数据
3. ✅ 确认数据完整
4. ✅ 可以删除旧版本

### 从IndexedDB版本迁移到Electron版本：

1. ✅ 在IndexedDB版本导出数据
2. ✅ 启动Electron应用
3. ✅ 使用Electron的导入功能（会自动打开文件选择对话框）
4. ✅ 数据自动保存到本地

---

## ⚠️ 注意事项

1. **IndexedDB版本**
   - 每个浏览器独立存储（Chrome的数据和Edge不共享）
   - 清除浏览器数据会删除笔记
   - 建议定期导出备份

2. **Electron版本**
   - 第一次需要安装Node.js和依赖包
   - 数据文件在系统AppData文件夹
   - 建议定期备份数据文件

3. **图片存储**
   - 所有版本都将图片转换为base64存储
   - 虽然有压缩，但大量高清图片仍会占用较多空间
   - 建议：
     - 图集内容优先使用外部图床链接
     - 重要图片才存储在应用内

---

## 💡 最佳实践

### 推荐使用流程：

1. **日常使用**：IndexedDB网页版（方便快捷）
2. **重要备份**：定期导出数据到电脑
3. **大数据量**：切换到Electron桌面版
4. **跨设备**：通过导出/导入功能同步数据

---

## 🆘 常见问题

**Q: IndexedDB版本还是提示存储配额不足？**
- A: 可能是浏览器限制，尝试：
  1. 清理浏览器缓存（保留站点数据）
  2. 使用Electron桌面版（无限制）

**Q: Electron版本如何更新？**
- A: 覆盖安装新版本即可，数据文件独立保存不会丢失

**Q: 数据文件在哪里？**
- A: Electron版在 `C:\Users\你的用户名\AppData\Roaming\my-notes-app\`
- IndexedDB版在浏览器内部数据库，通过导出功能获取

**Q: 如何完全卸载？**
- A: 
  1. 卸载应用程序
  2. 删除 `AppData\Roaming\my-notes-app` 文件夹（Electron）
  3. 清除浏览器站点数据（IndexedDB）

---

## 📞 技术支持

如有问题，请检查：
1. Node.js版本是否 >= 16
2. 网络连接是否正常（安装依赖时需要）
3. 系统防火墙是否阻止Electron

---

## 🎉 享受使用！

现在你有两个强大的版本可以选择，不用再担心存储配额问题了！

- 🌐 **网页版**：快速、方便、随时可用
- 💻 **桌面版**：强大、无限、完全本地

根据你的需求选择最适合的版本使用吧！
